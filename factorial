# 1. Сформувати функцію, що буде обчислювати факторіал заданого користувачем
# натурального числа n.
# Медуниця Андрій Олексійович | 122г

# Рекурсивний спосіб знаходження факторіала


def factorial(x):
    if x >= 0:                              # перевірка на додатнє число
        if x == 1:
            return 1
        else:
            total = x * factorial(x - 1)    # змінна яка виражає наше число, яке кожного разу буде меньше на одиницю
            return total                    # знову заходимо в рекурсію
    else:                                   # помилка якщо будуть введені не додатні значення, або не int тип данних
        raise ValueError('factorial() only accepts integral values')


n = int(input('x = '))                      # вводимо число
print(factorial(n))                         # виводимо число
########################################################################################################################

# Ітераційний спосіб знаходження факторіала

m = int(input('x_2 = '))        # вводимо число

fact = 1                        # завели змінну для добутку чисел в неї
k = 0
while k != m:                   # цикл працює поки k не буде == заданому числу m
    fact *= m - k               # домножаємо в fact різницю заданого числа m та k яке покроково збільшується на одиницю
    k += 1
print(fact)                     # виводимо факторіал
